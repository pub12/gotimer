// Purpose: Shared Header component for the Game Timer app. Reserved for future extensibility (e.g., navigation, branding).

import React from "react";

/**
 * header component for the Game Timer app. Currently empty, but can be extended for navigation or branding.
 */
export default function Header() {
  return (
    <header className="w-full"></header>
  );
} 