// Purpose: Shared Header component for the GoTimer app. Reserved for future extensibility (e.g., navigation, branding).

import React from "react";

/**
 * header component for the GoTimer app. Currently empty, but can be extended for navigation or branding.
 */
export default function Header() {
  return (
    <header className="w-full"></header>
  );
} 